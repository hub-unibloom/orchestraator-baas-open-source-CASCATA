worker_processes auto;
events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    # Tuning para Alta Performance (BaaS Grade)
    client_max_body_size 50M;
    proxy_buffers 16 16k;
    proxy_buffer_size 32k;

    # Backend Orchestrator
    upstream backend {
        server cascata-orchestrator:8080;
    }

    # Frontend Studio (Static Site serving on port 80 inside container)
    upstream frontend {
        server cascata-studio:80;
    }

    server {
        listen 80;
        server_name localhost;

        # System API (Auth, Dashboard Logic)
        location /system/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Tenant API (V1 Orchestration)
        location /v1/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Health & Telemetry
        location /health {
            proxy_pass http://backend;
            proxy_set_header Host $host;
        }

        # Frontend Studio (Dashboard UI)
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket Support for real-time (HMR/Vite)
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}
